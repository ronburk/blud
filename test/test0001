-- test0001
--
-- Simple test to make sure SOMETHING works

do
    BLUD_ASSERT_EXIT(1001, 'non_existent.o')
    : prog: non_existent.o
    :     gcc -o prog $(CFLAGS) non_existent.o
end
CFLAGS += -g
all: prog
    echo "test001 worked"
    touch test0001.out
