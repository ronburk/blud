-- test0001
--
-- Simple test to make sure SOMETHING works

TEST=foo
do
    assert($(TEST) == "foo")
    : prog: prog.o
    :     gcc -o prog $(CFLAGS) prog.o
end
CFLAGS += -g
all: prog
    echo "test001 worked"
    touch test0001.out
