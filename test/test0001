-- test0001 2026-07-23
--
-- Simple test to make sure SOMETHING works

do
    BLUD_ASSERT_EXIT(1000, 'non_existent.o')
    : prog: non_existent.o
    :     gcc -o prog $(CFLAGS) non_existent.o
end
CFLAGS += -g
talk:
    echo "echo 1"
    echo "echo 2"
all: talk prog
    echo "test001 worked"
    touch test0001.out
